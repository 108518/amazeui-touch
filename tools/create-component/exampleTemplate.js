import React from 'react';
import {
  Container,
  cptName
} from 'amazeui-touch';

const cptNameExample = (props) => (
  <Container {...props}>
    <cptName />
  </Container>
)

export default cptNameExample;
